package dev.hermes.manager.ui.theme

import androidx.compose.ui.graphics.Color

// Claude-inspired warm palette
val HermesOrange = Color(0xFFDA7756)      // Warm orange accent (Claude signature)
val HermesOrangeLight = Color(0xFFE8927A) // Lighter orange for hover
val HermesBg = Color(0xFF1A1918)          // Near-black warm background
val HermesSurface = Color(0xFF242220)     // Card / input surface
val HermesSurfaceVariant = Color(0xFF2D2B29) // Slightly lighter surface
val HermesOnSurface = Color(0xFFE8E3DD)   // Warm white text
val HermesOnSurfaceMuted = Color(0xFF9E9990) // Muted text / timestamps
val HermesUserBubble = Color(0xFF2D2B29)  // User message bubble
val HermesAiBubble = Color(0xFF1A1918)    // AI message (blends with bg)
val HermesDivider = Color(0xFF3A3835)     // Subtle divider
val HermesError = Color(0xFFE57373)
val HermesSuccess = Color(0xFF81C784)
